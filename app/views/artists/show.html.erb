<h1><%= @artist.name %> Songs</h1>

<table class="striped bordered">
  <tr>
    <th>Title</th>
    <th>Genre</th>
  </tr>

  <% @songs.each do |song| %>
      <tr>
        <td><%= song.title %></td>
        <td><%= song.genre %></td>
      </tr>
  <% end %>
</table>